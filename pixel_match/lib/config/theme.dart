// Legacy path — the brand system now lives in `lib/theme/`.
// This file re-exports the new tokens so older imports keep working while
// screens migrate to `package:pixel_match/theme/app_theme.dart`.
export '../theme/app_theme.dart';
